{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the GCS window (events + app commands).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
